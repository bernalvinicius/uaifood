import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
  root: {},
  divTitle: {
    marginBottom: '10px',
  },
  title: {
    fontSize: '18px',
    fontWeight: 400,
    color: '#484848',
  },
});
